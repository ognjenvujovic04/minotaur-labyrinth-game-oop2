#include "game.h"

Game::Game(int pRows, int pColumns, int pItemNumber) {
	colomns = pColumns;
	rows = pRows;
	itemNumber = pItemNumber;
	//mazeMatrix = maze_generator(pColumns, pRows, pItemNumber);
	
}